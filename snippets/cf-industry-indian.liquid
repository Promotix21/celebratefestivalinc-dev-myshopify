{% comment %} CF Industry: Indian Restaurants {% endcomment %}

{%- liquid
  # Indian Restaurant Equipment - using CORRECT Shopify handles from mapping
  assign categories = 'shaan-tandoor,rotoquip,indian-tandoor,tandoor-skewers,tandoor-spare-parts,aluminium-patila,kadai-all,taav-all,pressure-cooker-aluminum,idli-momo-steamer-all,cookware-cooking-ladles-all,new-theme-rice-cooker' | split: ','

  assign titles = 'Shaan Tandoor,Rotoquip Tandoor,Indian Tandoor,Tandoor Skewers,Tandoor Spare Parts,Patila,Kadai,Tava / Griddle,Pressure Cookers,Idli & Momo Steamers,Cooking Ladles,Rice Cookers' | split: ','

  assign images = 'shaan-tandoor.jpg,rotoquip-tandoor.jpg,indian-tandoor.jpg,tandoor-skewers.jpg,tandoor-parts.jpg,patila.jpg,kadai.jpg,tava.jpg,pressure-cooker.jpg,idli-steamer.jpg,cooking-ladles.jpg,rice-cooker.jpg' | split: ','
-%}

<h2 class="cf-industry-hub__section-title">Tandoor & Cooking Equipment</h2>

<div class="cf-industry-hub__grid">
  {%- for handle in categories limit: 6 -%}
    {%- assign idx = forloop.index0 -%}
    {%- assign title = titles[idx] -%}
    {%- assign collection = collections[handle] -%}

    <a href="/collections/{{ handle }}" class="cf-industry-hub__card">
      <div class="cf-industry-hub__card-image">
        {%- if collection != blank and collection.image -%}
          <img src="{{ collection.image | image_url: width: 400, height: 400, crop: 'center' }}" alt="{{ title }}" loading="lazy">
        {%- elsif collection != blank and collection.products_count > 0 -%}
          <div class="cf-industry-hub__slideshow">
            {%- for product in collection.products limit: 5 -%}
              {%- if product.featured_image -%}
                <img src="{{ product.featured_image | image_url: width: 400, height: 400, crop: 'center' }}" alt="{{ product.title }}" class="cf-industry-hub__slideshow-img{% if forloop.first %} active{% endif %}" loading="lazy">
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- else -%}
          <div style="width:100%;height:100%;background:linear-gradient(135deg,#f1f5f9 0%,#e2e8f0 100%);display:flex;align-items:center;justify-content:center;">
            <span style="font-size:14px;color:#94a3b8;">No Products</span>
          </div>
        {%- endif -%}
        <div class="cf-industry-hub__card-overlay">
          <span class="cf-industry-hub__card-overlay-text">{{ title }}</span>
        </div>
      </div>
      <div class="cf-industry-hub__card-content">
        <h3 class="cf-industry-hub__card-title">{{ title }}</h3>
        <span class="cf-industry-hub__card-btn">Shop Now</span>
      </div>
    </a>
  {%- endfor -%}
</div>

<h2 class="cf-industry-hub__section-title">Cookware & Utensils</h2>

<div class="cf-industry-hub__grid">
  {%- for handle in categories offset: 6 -%}
    {%- assign idx = forloop.index0 | plus: 6 -%}
    {%- assign title = titles[idx] -%}
    {%- assign collection = collections[handle] -%}

    <a href="/collections/{{ handle }}" class="cf-industry-hub__card">
      <div class="cf-industry-hub__card-image">
        {%- if collection != blank and collection.image -%}
          <img src="{{ collection.image | image_url: width: 400, height: 400, crop: 'center' }}" alt="{{ title }}" loading="lazy">
        {%- elsif collection != blank and collection.products_count > 0 -%}
          <div class="cf-industry-hub__slideshow">
            {%- for product in collection.products limit: 5 -%}
              {%- if product.featured_image -%}
                <img src="{{ product.featured_image | image_url: width: 400, height: 400, crop: 'center' }}" alt="{{ product.title }}" class="cf-industry-hub__slideshow-img{% if forloop.first %} active{% endif %}" loading="lazy">
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- else -%}
          <div style="width:100%;height:100%;background:linear-gradient(135deg,#f1f5f9 0%,#e2e8f0 100%);display:flex;align-items:center;justify-content:center;">
            <span style="font-size:14px;color:#94a3b8;">No Products</span>
          </div>
        {%- endif -%}
        <div class="cf-industry-hub__card-overlay">
          <span class="cf-industry-hub__card-overlay-text">{{ title }}</span>
        </div>
      </div>
      <div class="cf-industry-hub__card-content">
        <h3 class="cf-industry-hub__card-title">{{ title }}</h3>
        <span class="cf-industry-hub__card-btn">Shop Now</span>
      </div>
    </a>
  {%- endfor -%}
</div>

{% comment %} Link to full Indian Specialty section {% endcomment %}
<div style="text-align: center; margin-top: 30px;">
  <a href="/collections/indian-specialty" class="cf-industry-hub__card-btn" style="padding: 14px 32px; font-size: 16px;">
    View All Indian Specialty Equipment
  </a>
</div>
