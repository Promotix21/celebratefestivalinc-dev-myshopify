{
  "name": "cf-chatbot-backend",
  "version": "1.0.0",
  "description": "AI Kitchen Consultant Assistant backend for Celebrate Festival",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'no build step'",
    "migrate": "node db/migrate.js",
    "sync-products": "node db/sync-products.js",
    "weekly-report": "node scripts/weekly-report.js",
    "rollup": "node -e \"require('./lib/analytics').aggregateDay(new Date().toISOString().slice(0,10)).then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1);})\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "mysql2": "^3.12.0",
    "nodemailer": "^8.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
